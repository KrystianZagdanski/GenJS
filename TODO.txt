 TODO:
    Another:
    - Create `squer` class and rewrite colider to propperly detect colisions with them
    ?- Create `colider` class 
    - delete RColor() if not used
    - Change `pool` to be array with arrays with Unit id and fitnes score
    - 
    Unit:
    - Change `angle` to `transformation` or something
    - Add new varible that determine turn angle
    - Change `time` to umm something else like "entero somewhere elso" (`time` should be value 
        from start to win, the shortest the better)
    - Change fitness score added after win (it's too big)
    - Add fitness at the end insted of in every damn place
    - Change `fitnes` to `fitness`